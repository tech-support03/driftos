# ~/.config/quickshell/cava.conf — feeds the top-bar waveform.
[general]
bars = 44
framerate = 60
sleep_timer = 1

[input]
method = pulse
source = auto

[output]
method = raw
raw_target = /dev/stdout
data_format = ascii
ascii_max_range = 1000

[smoothing]
monstercat = 1
noise_reduction = 0.77
